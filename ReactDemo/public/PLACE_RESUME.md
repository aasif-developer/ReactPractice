Place your resume PDF here so the site can serve it at `/resume.pdf`.

Steps:
1. Add your resume file and name it `resume.pdf`.
2. Put the file in the `public/` directory.
3. Rebuild the site if needed (e.g., `npm run build`).

Once placed, the portfolio will make the resume available at `/resume.pdf` for download.